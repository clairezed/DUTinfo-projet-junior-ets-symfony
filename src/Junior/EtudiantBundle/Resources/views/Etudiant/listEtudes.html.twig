{% extends "JuniorEtudiantBundle:Etudiant:layoutEtudiant.html.twig" %}

{% block title %}
{{ parent()}} - Infos
{% endblock %}

{% block content_etudiant %}

<ul class="breadcrumb">
    <li class="active"><a href="#">Dashboard</a> <span class="divider">/</span></li>
</ul>
<h2>Mes études</h2>


<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nom Etude</th>
            <th>Prix Journee</th>
            <th>Indemnites</th>
            <th>Detail</th>
            <th>Acompte</th>
        </tr>
    </thead>
    <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.nomEtude }}</td>
                <td>{{ entity.prixJournee }}</td>
                <td>a calculer</td>
                <td><button class="btn btn-info btn-small" type="button"><a href="#">Voir l'etude</a></button></td>
                <td><button class="btn btn-warning btn-small" type="button"><a href="#">Demander un acompte</a></button></td>
                {#
                <td>
                <ul>
                    <li>
                        <a href="{{ path('etudiant_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('etudiant_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
                #}
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %} 