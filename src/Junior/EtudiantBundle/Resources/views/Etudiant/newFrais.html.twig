{% extends "JuniorEtudiantBundle:Etudiant:layoutEtudiant.html.twig" %}

{% block title %}
{{ parent()}} - Frais
{% endblock %}

{% block content_etudiant %}



<h2>Enregistrer de nouveaux frais</h2>

{#
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Etude</th>
            <th>Type frais</th>
            <th>Montant frais</th>
            <th>Date achat</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>todo</td>
            <td>todo</td>
            <td>todo</td>
            <td>todo</td>
            <td>todo</td>
            <td><button class="btn btn-small btn-success" type="button">+</button></td>
        </tr>
    </tbody>
</table>
#}
<div class="well">
    <form method="post" {{ form_enctype(form) }}>
        <div>
            {{ form_label(form.typeFrais, "Intitulé du frais :") }}
                {{ form_widget(form.typeFrais) }}
                {{ form_label(form.montantFrais, "Montant :") }}
                {{ form_widget(form.montantFrais) }}
                {{ form_label(form.dateAchat, "Date de la facture :") }}
                {{ form_widget(form.dateAchat) }}

        </div>
{{ form_widget(form) }}
            <input type="submit" class="btn btn-primary" />
                 <a class="btn btn-danger" href="{{ path('junior_etudiant_listFrais', { 'id': app.user.id }) }}">Annuler</a>
        </form>
   
    </div>
    


{#

    <form action="{{ path('etudiant_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('etudiant') }}">
            Back to the list
        </a>
    </li>
</ul>

#}

{% endblock %} 